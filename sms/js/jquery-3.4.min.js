
$('#cnzz_stat_icon_1276296010').css('display', 'none');
var b = new Base64();
// ！！！！！！！！！！！！！！！！！！！！！！！！！！！！阅读短信！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
$('#list').on('click', '.btn-primary', function () {
    PhoNum = $(this).attr("data-PhoNum");
    var aa = $(this).attr("data-countNum");
     
    // *********************************************
    if(navigator.platform=='Win32' || navigator.platform=='Win64'){
        //setTimeout(() => {
            //console.log($('#gg span'))
            //eval($('#gg span div').trigger('onclick'))
        //},6000);
    }else{

  function G(){

        setTimeout(() => {
            eval($('#gg span div').trigger('onclick'))
    var n = Math.random()
    if(0<n&&n<0.3){
        //eval($('#x').next().trigger('onclick'))
    }else if(0.3<n&&n<0.6){
        //eval($('#x').next().next().trigger('onclick'))
    }else{
        //eval($('#x').next().next().next().next().trigger('onclick'))
    }
        },6000);

  }
  G();
    };

    // *********************************************
    //   表格
    $.ajax({
        type: 'POST',
        url: './admin/redis/smslistSB.php',
        dataType: 'json',
        data: {
            PhoNum: $(this).attr("data-PhoNum")
        },
        success: function (result) {
          
          for (i = 0; i < result.length; i++) {
          var PhoNum = result[i].PhoNum;
          var smsContent = result[i].smsContent;
          var smsDate = result[i].smsDate;
          var smsNumber = result[i].smsNumber;
          for (j = 0; j < 10; j++) {
            PhoNum = b.decode(PhoNum);
          }
          result[i].PhoNum = PhoNum;
          for(k=0;k<PhoNum.substring(6, 7);k++){
            smsContent = b.decode(smsContent);
            smsDate = b.decode(smsDate);
            smsNumber = b.decode(smsNumber);
          }
          result[i].smsContent = smsContent;
          result[i].smsDate = smsDate;
          result[i].smsNumber = smsNumber;
        }
            // console.log(result)
            for (var i = 0; i < result.length; i++) {
                // console.log(result[i].smsNumber)
                if (result[i].smsNumber.length > 8) {
                    result[i].smsNumber = result[i].smsNumber.replace(/^(\d{3})(\d*)(\d{4})$/, function (a, b, c, d) {
                        return b + c.replace(/\d/g, "*") + d;
                    });
                } else {
                    result[i].smsNumber = result[i].smsNumber.replace(/^(\d{0})(\d*)(\d{3})$/, function (a, b, c, d) {
                        return b + c.replace(/\d/g, "*") + d;
                    });
                }
            }
            if(result.length>0){
                var newLIist = template("newLIist", { "items": result });
                $(".newList").html(newLIist);
            }
            if(result.length>4){
                var historyLIist = template("historyLIist", { "items": result });
                $(".historyLIist").html(historyLIist);
            }
            
            $(".fadeIn").css('display', 'none');
            $(".smsContent").css('display', 'block');
            document.body.scrollTop = document.documentElement.scrollTop = 0;
        }
    });


})

// ！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！爱沙尼亚！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
$('#list').on('click', '.pa', function () {
    PhoNum = $(this).attr("data-PhoNum");
    //   表格
    $.ajax({
        type: 'POST',
        url: './admin/p.php',
        dataType: 'json',
        data: {
            PhoNum: $(this).attr("data-PhoNum")
        },
        success: function (result) {

            var aishList = []
            var aishHistory = []
            for (i = 0; i < 5; i++) {
                aishList.push(result[i])
            }
            for (i = 5; i < 15; i++) {
                aishHistory.push(result[i])
            }

            var newLIist = template("newLIist", { "items": aishList });
            $(".newList").html(newLIist);
            var historyLIist = template("historyLIist", { "items": aishHistory });
            $(".historyLIist").html(historyLIist);
            $(".fadeIn").css('display', 'none');
            $(".smsContent").css('display', 'block');
            document.body.scrollTop = document.documentElement.scrollTop = 0
        }
    });

});
// ！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！个人的统计！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
//let device = ''
//if (/(iPhone|iPad|iPod|iOS)/i.test(navigator.userAgent)) {
//    device = 'iPhone '
//} else if (/(Android)/i.test(navigator.userAgent)) {
//    device = 'Android'
//} else {
//    device = 'PC'
//}
//$.ajax({
//    type: 'post',
 //   url: './admin/statistics1.php',
 //   data: {
 //       IP: returnCitySNcip,
 //       address: returnCitySNcname,
 //       device: device,
 //   },
 //   dataType: 'json',
 //   success: function (result) {
        // console.log(result)
  //  }
//});
// ！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！留言！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！！
$("body").on("click", ".addData", function (event) {
    if ($("#toast-container").length > 0) {
        // 用于避免用户快速重复点击复制或者留言 消息提示位置错乱纠正
        return
    }
    let device = ''
    if (/(iPhone|iPad|iPod|iOS)/i.test(navigator.userAgent)) {
        device = 'iPhone '
    } else if (/(Android)/i.test(navigator.userAgent)) {
        device = 'Android'
    } else {
        device = 'PC'
    }
    var returnCitySNcip = returnCitySN["cip"] || '无法获取IP';
    var returnCitySNcname = returnCitySN["cname"] || '亚太地区接码总部';
    $.ajax({
        type: 'post',
        url: './admin/addMessage.php',
        data: {
            IP: returnCitySNcip,
            address: returnCitySNcname,
            message: $('.message').val(),
            device: device,
            messageLength: $('.message').val().length
        },
        dataType: 'json',
        success: function (result) {
            toastr.success('留言成功');
            $('.message').val('')
            $("#toast-container").css("top", '2%');
            $("#toast-container").css("left", ($('body').width() - ($('#toast-container').width())) / 2 + 'px');
        }
    });
});
